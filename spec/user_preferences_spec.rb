require 'spec_helper'

describe 'UserPreferences' do
  it "loads a file from ~/.touchkey.conf" do
    pending
  end

  it "creates a file at ~/.touchkey.conf if such file does not exist" do
    pending
  end

  it "imports configuratino from ~/.touchkey.conf file" do
    pending
  end
end
