#!/usr/bin/env ruby

# LOAD_PATH is a ruby construct, works in a similar
# fashion as $PATH
$LOAD_PATH << "#{File.dirname(__FILE__)}/../lib"

require 'touchkey'

Touchkey::Key.grab_keyboard(Touchkey::EventDispatchers::SimpleDispatcher.new)
